# OMNIA frontend — copy to .env

# API base for the SPA. Leave EMPTY for preview/tunnel (same-origin /api via
# preview-server). Only set to http://127.0.0.1:43121 for local `yarn start`
# if you are NOT using the CRA proxy / setupProxy.
REACT_APP_BACKEND_URL=
PORT=43122
